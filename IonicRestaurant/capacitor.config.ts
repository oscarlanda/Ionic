import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.barrioslanda.IonicRestaurant',
  appName: 'IonicRestaurant',
  webDir: 'www'
};

export default config;
